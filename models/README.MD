# Models

This folder is used to store the trained reinforcement learning models for the local SAC grasp-correction policy.

The main integrated demo expects a trained model file similar to:

```text
sac_youbot_grasp.zip
